# Backend Configuration
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000

# CORS Configuration
# Add frontend URLs that should be allowed to access the API
CORS_ALLOW_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://frontend:80

# Environment
ENVIRONMENT=development
DEBUG=true

