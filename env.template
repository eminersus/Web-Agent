# =============================================================================
# Web Agent Configuration
# Architecture: LibreChat -> MCP Server (FastMCP with SSE)
#               + Backend Middleware (for monitoring and future flow control)
# =============================================================================

# -----------------------------------------------------------------------------
# OpenRouter Configuration (LLM Provider)
# -----------------------------------------------------------------------------
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_DEFAULT_MODEL=anthropic/claude-3.5-sonnet
OPENROUTER_TIMEOUT=120.0

# -----------------------------------------------------------------------------
# LibreChat Configuration
# -----------------------------------------------------------------------------
# Application URL (used for OpenRouter referer header)
APP_URL=http://localhost:3080

# Session Configuration (in milliseconds)
SESSION_EXPIRY=900000
REFRESH_TOKEN_EXPIRY=604800000

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
# Generate secure random strings for these values
JWT_SECRET=your_jwt_secret_change_this_in_production
JWT_REFRESH_SECRET=your_jwt_refresh_secret_change_this_in_production
NEXTAUTH_SECRET=your_nextauth_secret_change_this_in_production
NEXTAUTH_URL=http://localhost:3080

# Registration Settings
ALLOW_REGISTRATION=true
ALLOW_EMAIL_LOGIN=true

# -----------------------------------------------------------------------------
# MCP Server Configuration (FastMCP with SSE)
# -----------------------------------------------------------------------------
MCP_HOST=0.0.0.0
MCP_PORT=8001
DEV_MODE=false
MCP_SERVER_URL=http://mcp-server:8001
MCP_TIMEOUT=60.0

# -----------------------------------------------------------------------------
# Backend Middleware Configuration
# -----------------------------------------------------------------------------
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000

# CORS Configuration
# For production, specify exact origins (comma-separated)
# For development, use *
CORS_ALLOW_ORIGINS=*

# Environment
ENVIRONMENT=development
DEBUG=true

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# MongoDB connection string
MONGO_URI=mongodb://mongo:27017/librechat

# -----------------------------------------------------------------------------
# eBay API Configuration
# -----------------------------------------------------------------------------
# Get your credentials from: https://developer.ebay.com/
# 
# OPTION 1: Manual OAuth Token (expires after ~2 hours)
# EBAY_APP_ID=your_oauth_token_here
#
# OPTION 2: Automatic Token Refresh (RECOMMENDED)
# Set both CLIENT_ID and CLIENT_SECRET for automatic token management
EBAY_CLIENT_ID=your_ebay_client_id_here
EBAY_CLIENT_SECRET=your_ebay_client_secret_here
#
# Sandbox for testing (set to true to use sandbox environment)
EBAY_USE_SANDBOX=false

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
